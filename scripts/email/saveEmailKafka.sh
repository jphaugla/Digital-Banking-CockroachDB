# save a sample transaction
echo "this is not currently working-code is there but not the jdbc sink driver"
#curl -X POST -H "Content-Type: application/json" http://localhost:8080/api/email/saveKafka?email --data @email.json
